# ---------------------------------------------------------------------------
# API
# ---------------------------------------------------------------------------
# Debe coincidir con el puerto publicado en docker-compose.yml (3050:3050).
PORT=3050
API_PREFIX=api
CORS_ORIGIN=http://localhost:4200

# ---------------------------------------------------------------------------
# Base de datos principal (documentación) — equivale a la conexión "mysql"
# ---------------------------------------------------------------------------
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=documentacion
DB_USERNAME=root
DB_PASSWORD=

# ---------------------------------------------------------------------------
# Base de datos institucional SAF — equivale a la conexión "mysqlSaf"
# (usuarios, servidores públicos, dependencias, direcciones, departamentos)
# ---------------------------------------------------------------------------
DB_SAF_HOST=127.0.0.1
DB_SAF_PORT=3306
DB_SAF_DATABASE=adminplem_saf
DB_SAF_USERNAME=root
DB_SAF_PASSWORD=

# ---------------------------------------------------------------------------
# Seguridad
# ---------------------------------------------------------------------------
JWT_SECRET=cambia-esta-clave-en-produccion
JWT_EXPIRES_IN=8h
MAX_INTENTOS_LOGIN=3

# ---------------------------------------------------------------------------
# Almacenamiento de documentos (equivalente a storage/app de Laravel)
# ---------------------------------------------------------------------------
STORAGE_PATH=./storage

# ---------------------------------------------------------------------------
# Firma electrónica institucional (FIEL)
# ---------------------------------------------------------------------------
FEPLEM_URL=https://feplem.gob.mx
FEPLEM_TIMEOUT_MS=30000
